# Passport Local Authentication

## Packages

* bcrypt
* body-parser
* cors
* express
* express-session
* mongoose
* nodemon
* passport
* passport-local

## Quick Start

`npm cache verify`
`npm cache clean --force (Optional)`
`npm install`
`npm start`